---
description: Always practice ethical Ascension calc RE; never invent coefficients; duplicate abilities share one GCD
alwaysApply: true
---

# Ascension calculation reverse engineering (ethical)

User mandate: **always** practice Ascension calc RE as part of Buildschmiede work.

**Audience (first-class both):** useful for **level-run 10–59** and a complete
**L60 / endgame** builder (Wildcard at 60 included). See `AGENTS.md`.

When level-run and L60 numbers or priorities conflict: prefer solutions that
serve **both**, or gate by UI mode (Levelrun / Endgame). Do **not** discard L60
accuracy to favor level-run alone.

How agents work / ship: `.cursor/rules/full-agent-utilization.mdc`.

## Shared GCD (duplicate abilities) — always

User mandate: **Duplicate abilities always share the GCD.**

Several catalog entries for the **same ability**, and **school variants that share
a GCD slot**, occupy **one** global cooldown — they are **not** stackable in
parallel. Treat this as hard truth in Analyse, Wirkungsketten, `methods`,
generator, and docs.

**Not the same as** talent inheritance (already in `AGENTS.md` → Spielmechanik):

| Rule | Means |
|---|---|
| **Talent inheritance** | „uses X modifiers“ → school variant inherits **talents** of base X; base need not be in the build |
| **Shared GCD** | Same ability / shared GCD-slot variants → **one GCD**, not two independent presses in the same window |

Do not score, chain, or recommend as if duplicates grant parallel GCDs.

## Do (ethical RE)

- Mine **AscensionDBC**, tooltip text, and **Safe** live Ascension APIs for real numbers:
  CD, cost, range, cast, charges, weapon %, flats from description text
- Improve `sync_tooltips`, `scaling`, `methods`, `mechanics` (and related pipeline) from measured data
- **Never invent** SP/AP coefficients (or other ratios) missing from data — omit or mark missing
- Keep duplicate / shared-GCD abilities as **one** GCD in timing and density logic
- Document findings in `pipeline/NOTES-*.md`
- Read-only client extracts for research are fine; derived JSON/notes go in repo

## Don't

- Invent coefficients / guess SP–AP ratios / fabricate tooltip numbers
- Treat duplicate ability entries or shared-GCD school variants as parallel GCDs
- Commit proprietary Ascension/Blizzard client Lua
- Exe decompile, anticheat bypass, memory hacks, or other client-tampering RE

## Anchors

- `AGENTS.md` → Zielgruppe 10–60 (Levelrun + Endgame); „Nichts raten…“; Spielmechanik (shared GCD vs talent inheritance)
- Dual audience / ship workflow: `.cursor/rules/full-agent-utilization.mdc`
- Pipeline notes: `pipeline/NOTES-*.md`
- Auto-allow read-only DBC/API research: `full-agent-utilization.mdc`
